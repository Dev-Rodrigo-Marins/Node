<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Galeria de Relatos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/relatos">Galeria</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <% if (user) { %>
            <li class="nav-item">
              <img src="<%= user.foto %>" alt="Foto" class="rounded-circle" width="50" height="50">
            </li>
            <li class="nav-item mx-2 text-white d-flex align-items-center"><%= user.nome %></li>
            <li class="nav-item">
              <a class="nav-link" href="/relatos">Relatos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/auth/logout">Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/google">Login com Google</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
